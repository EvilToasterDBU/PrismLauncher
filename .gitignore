Thumbs.db
*.kdev4
.user
.directory
resources/CMakeFiles
*~
*.swp
html/

# Project Files
*.pro.user
CMakeLists.txt.user
CMakeLists.txt.user.*
CMakeSettings.json
/CMakeFiles
CMakeCache.txt
CMakeUserPresets.json
/.project
/.settings
/.idea
/.vscode
/.vs
cmake-build-*/
Debug
compile_commands.json

# Build dirs
build
/build-*

# Install dirs
install
/install-*

# Ctags File
tags

# YouCompleteMe config stuff.
.ycm_extra_conf.*

#OSX Stuff
.DS_Store

branding/
secrets/
run/

.cache/

# Nix/NixOS
.direnv/
## Used when manually invoking stdenv phases
outputs/
## Regular artifacts
result
result-*
repl-result-*

# Flatpak
.flatpak-builder
flatbuild

# Snap
*.snap
bigscreen/CLAUDE.md

# Local Steamworks dev/test artifact for a build dir/anywhere else a dev
# might drop one — a real AppID picked per-machine/per-testing-session, not
# project config. NOT the tracked bigscreen/packaging/steam_appid.txt below
# (that one ships in the actual package — a real device turned out to need
# it even when launched through Steam as a non-Steam game shortcut, not
# just for a bare local dev run — see bigscreen/CLAUDE.md).
steam_appid.txt
!bigscreen/packaging/steam_appid.txt
